# 計畫:以申報規範矩陣為中心的異動偵測

## 決策前提(已確認)

1. **條文監控保留為系統核心**,角色是**觸發器**,不是產出。
2. **不需要精確映射「哪份公告對應哪條法條」**。多對多的條文級錨定
   (`supp=0`、`G3 to_article_level=0`)不再是必要工作。
3. **出處保留到文件層級** —— 不必到條號,但要能回答「這個 5% 來自哪份公告」。
4. **矩陣內容改由 GPT Researcher 產出**,經 `import_workbook()` 或等效路徑入庫。
5. **Gateway 約束**:WSO2 APIM 懲罰長時間單一請求。實測 6m41s 的請求會觸發
   backend timeout → 502 → endpoint suspension → 後續一律 400。GPT Researcher
   走同一座 gateway 但因為是多次小請求,實測不觸發。
   **所有 LLM 使用一律往「多次小請求」設計,不得退化為單次大請求。**

## 架構

```
條文監控(現有)
  偵測到該稅種家族任一文件內容雜湊改變
   ↓ 觸發(粗粒度)
重新產生該稅種矩陣(GPT Researcher)
   ↓
與現行矩陣比對,差異寫入 RequirementFieldHistory
   ↓
「小規模納稅人-簡易計稅-出租住房 · 徵收率:3%減按2% → 3%減按1%」
 附:本次涉及《財稅公告2026年第3號》等 2 份文件
```

---

## 階段 0 — 受影響情境視圖(先上線,不依賴其他階段)

條文異動時,反查現有矩陣中引用該 `node_key` 的 `RequirementField`,
以**申報作業**為單位呈現,而非條文 diff。

- 建立在既有 `flag_stale_fields()` 的 `cited_node_keys` 比對上
- **不改資料表、不等 GPT Researcher、不承擔標籤分類風險**
- Changes 頁面的主標題改為「受影響的申報情境」

呈現樣式:

```
【增值稅】小規模納稅人 - 簡易計稅(銷售舊貨等)
 · 欄位:徵收率(現值:3% 減按 2%)
 · 異動依據:《財稅公告 2026 年第 3 號》
 · 狀態:待覆核
```

**這是唯一能在短期內讓財務人員受益的項目,優先做。**

---

## 階段 1 — GPT Researcher 原型

- 新增 `taxwatch/research/`,以稅種為單位發動研究。
- 產出對映到 `FIELD_SPECS` 的欄位;`FieldSource` 新增 `RESEARCH`。
- 沿用既有優先序:人工編輯不被自動產出覆蓋。
- **目的是觀察真實產出的情境顆粒度**,作為階段 2 標籤設計的依據。
  在看到真實產出之前不要定案標籤集合。

---

## 階段 1 補充 — 匯入層的已知缺陷

以下兩點在串接 GPT Researcher 前必須處理,否則會靜默失敗。

### 英文表頭會被丟棄

`taxwatch/requirements/importer.py:40` 的 `_HEADER_HINTS` 除 `requirement`
外**全為中文**。英文表頭匯入時只有 `Requirement` 對得上,其餘十欄無聲丟棄
—— 不報錯,得到一張看似匯入成功但幾乎空白的矩陣。

處置:補齊英文 hint,或要求 GPT Researcher 統一產出中文表頭。
**無論選哪個,匯入結果都必須回報「未對映到的表頭」清單**,
讓靜默丟棄變成可見的警告。

### 已驗證可對映的欄位

真實產出的 16 欄中,11 欄與現有 `FIELD_SPECS` 一對一對應:
`taxable_event`、`rate`、`taxable_items`、`formula`、`tax_base`、
`deductions`、`incentives`、`filing_deadline`、`payment_deadline`、
`administration`,以及語意略有出入的 `applicability`。

另兩欄需要特別處理:

- **`Policy Basis & Remarks`** —— 產出包含條號級參照
  (`Income Tax Act Art. 88, 92`)。這代表 GPT Researcher **有能力產出條文層級出處**,
  而 `taxwatch/graph/citation.py` 的 `extract_citations()` 本就在做這件事。
  **階段 1 必須驗證:能否把這類自由文字解析回 `node_key`。**
  若可行,失效標記機制得以保留,不必只靠矩陣對矩陣 diff ——
  這會顯著提高整體可信度,優先度高。
- **`Change Content`** —— 混雜不同年度的修法敘述且**無日期維度**,
  是背景說明而非異動事件。**不得用於異動偵測**,只能作為人工參考。

---
## 階段 2 — 矩陣身分穩定化

### 為什麼兩層標籤不夠(實例)

| 身分組合 | 實際稅率 | 結果 |
|---|---|---|
| 一般納稅人 / 一般計稅 / 貨物 | 一般貨物 13%、農產品與圖書 9% | 9% 覆蓋 13% |
| 小規模納稅人 / 簡易計稅 / 不動產 | 銷售不動產 5%、出租住房 3%減按1.5% | 5% 覆蓋 1.5% |

在稅法中「適用子項目」本身就是課稅實體的一部分,不能移出身分。

### 身分定義

```
country + tax_key + taxpayer_role + dimension_1 + dimension_2 + scenario_key
```

**兩個維度的詞彙依稅種而定,不是全域共用。** 這是看過真實產出後修正的設計 ——
原先設想的 `tax_scheme` × `target_category` 是加值稅專屬概念,套不到所得稅:
扣繳、結算申報、未分配盈餘不屬於「一般計稅/簡易計稅」任何一項;
技術服務報酬、權利金、股利是**所得類別**而非標的類別。

| 稅種 | 維度 1 | 維度 2 |
|---|---|---|
| 增值稅 / 營業稅 | 計稅方式(一般計稅/簡易計稅/免稅/零稅率/退稅) | 標的類別(貨物/勞務服務/不動產/無形資產/進出口) |
| 所得稅 | 課稅方式(扣繳/結算申報/未分配盈餘/暫繳) | 所得類別(技術服務報酬/權利金/股利/營業所得…) |
| 其他稅種 | 待該稅種有真實產出後再定 | 同左 |

- 詞彙表以稅種為單位維護,**不得為了統一而強行合併**。
- 新稅種在看到真實產出之前不定義詞彙。
- `scenario_key`:受控 slug,在上述兩維度之下再區分具體子項目
  (增值稅 `real_estate_lease_residential`、`used_goods`;
   所得稅 `technical_service_fee`、`royalty`、`dividend`)。
- `scenario`(自由文字)降級為人類可讀描述,不參與身分。

### scenario_key 的治理(關鍵)

若讓模型自由發明 slug,漂移問題只是從中文換成英文。因此:

- 維護一份 **slug 註冊表**,已存在的 slug 必須沿用
- 模型可提議新 slug,但需標記待人工確認後才進註冊表
- 這是 **Baseline Pinning 套用在 slug 上**:已覆核與已匯入的列,
  其 slug 永久固化

### 扣繳的歸屬(待決策)

`Withholding Tax (Non-Resident)` 在現行 taxonomy 沒有對應,`tw_collection`
的關鍵字也不含「扣繳」。它不是稅種而是**課徵機制**,橫跨營所稅與綜所稅。

兩個選項:獨立成 `tax_key`,或歸進所得稅並以「課稅方式」維度區分。
**傾向後者** —— 扣繳的稅率與申報義務終究由所得稅法規定,獨立成稅種會讓
同一部母法的異動分散在兩個稅種底下。

---

## 既有 PR 的處置

- **#37 合併** —— 重試與 suspension 退避是 LLM 呼叫的共通基礎設施。
- **#38 擱置** —— 交叉參照骨架讓每批請求從 20k 增為 30–35k,在懲罰長請求的
  gateway 底下是負資產;且抽取管線將退居二線。

## 不要做的事

- 不得放寬既有防捏造機制(`_verify_citations` 對 node_key 的驗證)
- 不得用字串相似度自動合併規範列
- 不得刪除規範列來讓列數變好看
- 不得設計成「單次大請求」
- **在看到某稅種的真實產出之前,不得定案該稅種的標籤詞彙**
- **不得為了統一而把不同稅種的維度詞彙強行合併** —— 加值稅的計稅方式與
  所得稅的課稅方式是不同的概念,共用一組枚舉會兩邊都不合身
- **不得以 `Change Content` 欄作為異動偵測來源**

## 執行順序

階段 0 → 階段 1 → 階段 2 → 階段 3。
階段 0 不依賴任何其他階段,可立即開始。
